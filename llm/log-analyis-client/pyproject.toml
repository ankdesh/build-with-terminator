[project]
name = "log-analysis-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "logparser3>=1.0.4",
    "pandas>=3.0.2",
    "pybind11>=3.0.4",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
