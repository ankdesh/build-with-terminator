#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

args=()
for arg in "$@"; do
  case "$arg" in
    -c) args+=("-s") ;;
    *) args+=("$arg") ;;
  esac
done

exec "${repo_root}/.venv/bin/yowasp-yosys" "${args[@]}"
